# This project is deployed via Cloudflare Pages (not Workers).
# No wrangler.toml configuration is needed for Pages deployment.
# Kept here for reference only.
name = "paws-business-case-modeller"
compatibility_date = "2024-01-01"
